#include <bits/stdc++.h>
using namespace std;
class Solution {
public:
    string removeOccurrences(string s, string part) {
        int i = 0;
        int j = 0;
        string ans = "";
        while(i + part.length() < s.length())
        {
            if(i )
        }
    }
};
int32_t main()
{
    #ifdef LOCAL_PROJECT
        freopen("input.txt", "r", stdin);
        freopen("output.txt", "w", stdout);
    #endif
    vector<int> a = {1, 1, 1};
    Solution sol;
    // cout << sol.canBeIncreasing(a) << endl;
    return 0;
}